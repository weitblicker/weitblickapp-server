Public sql_getText(Integer languageNo,Integer TextNo) {
//pulls a text from sql weitblick.texts by textNo and languageNo

}


Public sql_setText(Integer languageNo,String Text){
//checks if the text already exists and returns the number
//or adds the text with a new number

textNo = sql_searchText(languageNo,Text)
if not textNo {
    return sql_addText(languageNo,Text)
} else {
    return textNo
}

Public sql_searchText(languageNo,Text){
//searches a text in the weitblick.texts table and returns its number if found
//returns 0 for no results

return textNo
}

Public sql_addText(languageNo,Text){
//adds a new text to the weitblick.texts table

}